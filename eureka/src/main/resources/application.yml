eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      #      defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/
    register-with-eureka: false
  server:
    enable-self-preservation: false #只可以设置在开发环境，生产环境不可关闭
spring:
  application:
    name: eureka
server:
  port: 8761