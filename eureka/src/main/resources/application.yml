eureka:
  client:
    service-url:
     defaultZone: http://localhost:8761/eureka
    register-with-eureka: false #禁用它的客户端注册行为。
    fetch-registry: false  #禁用它的客户端注册行为。

spring:
  application:
    name: eureka

server:
  port: 8761